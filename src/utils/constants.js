const AUTH_STORAGE_KEY = 'wa_cert_authenticated';

const DARK_MODE_STORAGE_KEY = 'is_dark_mode_on';

const REQUEST_API_TYPES = {
  SEARCH_BY_TERM: 'search_by_term',
  SEARCH_RELATED_VIDEOS: 'search_related_videos',
  VIDEO_INFO: 'video_info',
};

export { AUTH_STORAGE_KEY, DARK_MODE_STORAGE_KEY, REQUEST_API_TYPES };
